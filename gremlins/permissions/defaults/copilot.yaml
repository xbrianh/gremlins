# Native Copilot permission block. bypass: true maps to --allow-all at invocation time.
# Full native-config wiring (AGENT.md / .github/copilot-instructions.md) lands in #788.
allowed_tools:
  - read
  - edit
  - bash
  - write
  - grep
  - glob
