Push the current branch with 'git push -u origin <branch-name>' and open a PR against the default branch with 'gh pr create --base <default-branch> --title "<title>" --body "<body>"'. You MUST pass an explicit --body so the close-link below lands in the PR description (do not rely on `gh pr create` auto-generating a body, and do not use `--fill`). Print ONLY the PR URL on the final line of your response.
