You are on a detached HEAD. Compute a branch name as 'issue-<N>-<short-slug>' if there is an issue number, otherwise a short descriptive slug. Push with 'git push origin HEAD:refs/heads/<branch-name>' then write a JSON file to '{session_dir}/pr-draft.json' with fields: "head" (the branch name you pushed), "base" ("{base_ref}"), "title" (a concise PR title), and "body" (the full PR description in markdown). Do NOT run 'gh pr create'. Print ONLY "Draft written." on the final line of your response.
