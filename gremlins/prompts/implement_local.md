{spec}{plan}

Implement every task in the plan above by editing code in this repo. You MUST commit all changes before finishing. Use `git add` + `git commit` as you work; multiple commits are encouraged for traceability. Do NOT create any meta/scaffolding files in the repo — no `.claude-workflow/` directory, no `plan.md`, no review docs, no notes-to-self. Do not push.
