name: local

default_client: claude:sonnet

stages:
  - { name: plan,         type: plan,         prompt: [code_style.md, plan.md] }
  - { name: implement,    type: implement,    prompt: [code_style.md, implement_local.md] }
  - { name: review-code,  type: review-code,  prompt: [code_style.md, review/detail.md] }
  - { name: address-code, type: address-code, prompt: [code_style.md, address.md, bail_section.md] }
  - { name: verify,       type: verify,       prompt: [verify_fix.md, bail_section_fix.md] }
