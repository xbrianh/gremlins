[package]
name = "gremlins-core"
version = "0.0.0"
edition = "2021"

[lib]
name = "gremlins_core"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = "0.29"
libc = "0.2"

[features]
extension-module = ["pyo3/extension-module"]
