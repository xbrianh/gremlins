stages:
  - type: sequence
    body:
      - name: detach-prior-pr
        type: exec
        options:
          cmds:
            - |
              if [ -f "$GREMLINS_ARTIFACT_DIR/pr-branch.txt" ]; then
                branch="$(cat "$GREMLINS_ARTIFACT_DIR/pr-branch.txt")"
                if [ -n "$branch" ]; then
                  git fetch origin "refs/heads/$branch:refs/remotes/origin/$branch" && git checkout --detach "origin/$branch"
                fi
              fi
      - name: handoff-init
        type: exec
        skip_if_exists: "artifact://rolling-plan.md"
        bind:
          rolling_plan: "artifact://rolling-plan.md"
          boss_spec: "artifact://boss-spec.md"
        options:
          cmds:
            - |
              if [ ! -f "{boss_spec}" ]; then
                cp "$GREMLINS_ARTIFACT_DIR/plan.md" "{boss_spec}"
              fi
              if [ ! -f "{rolling_plan}" ]; then
                cp "$GREMLINS_ARTIFACT_DIR/plan.md" "{rolling_plan}"
              fi

      - name: handoff
        type: agent
        prompt:
          - "gremlins:handoff.md"
          - "gremlins:code_style.md"
        interpolation:
          plan_text: content("artifact://rolling-plan.md")
          spec_section: content("artifact://boss-spec.md")?
          rolling_plan: artifact://rolling-plan.md
        bind:
          child_plan:   "artifact://child-plan.md"
          signal:       "artifact://signal.json"
        options:
          idle_timeout: 3600

      - name: translate-signal
        type: exec
        interpolation:
          signal: artifact://signal.json
          rolling_plan: artifact://rolling-plan.md
          child_plan: artifact://child-plan.md
        bind:
          bail?: "artifact://bail"
          done?: "artifact://done"
        options:
          cmds:
            - |
              SIGNAL="{signal}"
              ROLLING="{rolling_plan}"

              cp "$ROLLING" "$GREMLINS_ARTIFACT_DIR/rolling-plan-pre-sanitize.md"

              if [ ! -f "$SIGNAL" ]; then
                printf 'signal.json not written by agent' > "{bail}"
                exit 2
              fi
              exit_state=$(jq -r '.exit_state // empty' "$SIGNAL") || {
                printf 'failed to parse exit_state from signal.json' > "{bail}"
                exit 2
              }
              case "$exit_state" in
                next-plan)
                  CHILD="{child_plan}"
                  if [ ! -f "$CHILD" ]; then
                    printf 'next-plan: child_plan not found: %s' "$CHILD" \
                      > "{bail}"
                    exit 2
                  fi
                  cp "$CHILD" "$GREMLINS_ARTIFACT_DIR/plan.md"
                  ;;
                chain-done)
                  cp "$GREMLINS_ARTIFACT_DIR/boss-spec.md" "$GREMLINS_ARTIFACT_DIR/plan.md"
                  printf 'done' > "{done}"
                  ;;
                bail)
                  jq -r '.reason // "(no reason given)"' "$SIGNAL" \
                    > "{bail}" || \
                    printf '(jq failed reading bail reason)' > "{bail}"
                  exit 2
                  ;;
                *)
                  printf 'unrecognized exit_state: %s' "$exit_state" \
                    > "{bail}"
                  exit 2
                  ;;
              esac

      - name: sanitize
        type: agent
        prompt:
          - "gremlins:handoff_sanitize.md"
          - "gremlins:code_style.md"
        interpolation:
          rolling_plan_text: content("artifact://rolling-plan.md")
          rolling_plan: artifact://rolling-plan.md
        options:
          model: haiku

      - name: restore-rolling-plan
        type: exec
        interpolation:
          rolling_plan: artifact://rolling-plan.md
        options:
          cmds:
            - |
              ROLLING="{rolling_plan}"
              if [ ! -s "$ROLLING" ]; then
                cp "$GREMLINS_ARTIFACT_DIR/rolling-plan-pre-sanitize.md" \
                  "$ROLLING"
              fi
